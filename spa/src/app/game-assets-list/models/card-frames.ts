export class CardFrames {
    townCenters: Promise<string[]> = Promise.resolve([]);
    buildings: Promise<string[]> = Promise.resolve([]);
    minions: Promise<string[]> = Promise.resolve([]);
    actions: Promise<string[]> = Promise.resolve([]);
}
